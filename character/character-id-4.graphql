import { gql } from '@apollo/client';

const GET_USER = gql`
  query GetUser($id: ID!) {
    user(id: $id) {
      "id": 4,
      "name": "Mace Windu",
      "status": "active",
      "species": "human",
      "type": "jedi",
      "gender": "male"
    }
  }
`;
