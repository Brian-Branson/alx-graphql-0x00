import { gql } from '@apollo/client';

const GET_USER = gql`
  query GetUser($id: ID!) {
    user(id: $id) {
      "id": 3,
      "name": "Luke Skywalker",
      "status": "active",
      "species": "human",
      "type": "jedi",
      "gender": "male"
    }
  }
`;
