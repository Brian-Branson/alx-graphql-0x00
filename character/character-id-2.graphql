import { gql } from '@apollo/client';

const GET_USER = gql`
  query GetUser($id: ID!) {
    user(id: $id) {
      "id": 2,
      "name": "Obi-Wan Kenobi",
      "status": "active",
      "species": "human",
      "type": "jedi",
      "gender": "male"
    }
  }
`;
